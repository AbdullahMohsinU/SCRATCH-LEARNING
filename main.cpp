#include "cook.h"

int main() {
    cook("Start\n");

    check(10 > 5) {
        cook("CHECK passed!\n");
    }
    dash;

    run (int i to 0; i until 223; ++i) {
        cook("RUN loop says hi\n");
    }

    return 0;
}

